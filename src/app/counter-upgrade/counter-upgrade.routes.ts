import { Routes } from '@angular/router';
import { CounterComponent } from './counter.component';

export const COUNTER_UPGRADE_ROUTES: Routes = [
  {
    path: '',
    component: CounterComponent,
  },
];
